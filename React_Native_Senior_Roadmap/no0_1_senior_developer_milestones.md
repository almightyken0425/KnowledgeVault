# 專業級開發者學習與檢核藍圖

## 核心目標
- 建立從基礎元件到全域狀態的完整知識體系
- 具備獨立檢閱商業專案程式碼之專業能力
- 導入實務常用套件與進階開發模式

---

## 基礎架構與跨螢幕排版佈局
- **屬性:** 涵蓋畫面構成與基礎排版
- 認識核心運作機制
  - **行為:**
    - [x] 設定跨平台開發環境
    - [x] 剖析 React 與 React Native 渲染差異
    - [x] 遵循 JSX 編寫規則
- 熟悉核心基礎元件
  - **行為:**
    - [x] 運用 `View` 建構區塊容器
    - [x] 使用 `Text` 渲染並格式化文字
    - [x] 透過 `Image` 載入本地與遠端圖片
- 實踐 Flexbox 排版策略
  - **行為:**
    - [x] 控制 flexDirection 決定主軸行列方向
    - [x] 使用 justifyContent 與 alignItems 達成精準對齊
    - [x] 處理各種螢幕尺寸與螢幕旋轉方向
    - [x] 透過 `StyleSheet` 將樣式與邏輯分離

---

## 元件化與資料傳遞
- **屬性:** 提升程式碼片段之重用率
- 設計可重複使用之元件
  - **行為:**
    - [x] 運用屬性於上下層元件間傳遞資料
    - [x] 規劃合理的元件階層與單向資料流
    - [x] 實作共用按鈕與資料展示卡片模組

---

## 狀態管理與 React Hooks 總覽
- **屬性:** 從單一元件狀態到邏輯抽離與效能優化
- 基礎狀態與副作用
  - **行為:**
    - [x] 運用 useState 處理單一資料變更
    - [x] 透過 TextInput 收集用戶輸入並雙向綁定狀態
    - [x] 運用 useEffect 處理網頁載入或資料獲取等非同步行為
    - [x] 精準設定依賴陣列以避免無限迴圈
    - [x] 實作清理函式以釋放記憶體資源
- 重構與進階狀態管理
  - **行為:**
    - [x] 評估並比較 Redux 與 Context 等不同架構之適用場景
    - [x] 引入 useReducer 集中管理複雜或多重關聯狀態
    - [x] 建立 Custom Hooks 將商業邏輯從 UI 渲染層抽離
- 掌握進階效能優化策略
  - **行為:**
    - [x] 運用 useMemo 記憶費時之運算結果
    - [x] 運用 React.memo 單純阻擋基本型別屬性以避免不必要的子元件重繪
    - [x] 運用 useCallback 穩定函式參考以證明單兵作戰無法阻擋重繪
    - [x] 結合 React.memo 與 useCallback 達成完美組件防禦並解決傳遞函式時的效能地雷
- 第三方套件整合與路由管理
  - **行為:**
    - [x] 配置 Expo Router 或 React Navigation 建立多頁面導航
    - [x] 處理深層連結 Deep Linking 以支援外部喚醒特定頁面
## 專屬行動裝置生態適配
- **屬性:** 處理手機特有行為與效能瓶頸
- 優化長列表效能
  - **行為:**
    - [x] 將效能不佳之渲染替換為 `FlatList`
    - [x] 設定唯一鍵值以協助底層比對差異
  - **行為:**
    - [x] 使用 `KeyboardAvoidingView` 確保輸入框在鍵盤彈出時維持可見
    - [x] 運用 `SafeAreaView` 避開系統狀態列與底部橫條

---

## 導航系統與動態路由
- **屬性:** 建構多頁面層級架構
- 建立基礎導航結構
  - **行為:**
    - [x] 實作堆疊導航處理前後頁面跳轉
    - [x] 實作底部標籤導航處理平行主要功能區塊
    - [x] 設置標題區塊中之操作按鈕
- 處理跨頁面參數交互
  - **行為:**
    - [ ] 於路由導航時夾帶動態參數
    - [ ] 根據傳遞參數動態修改標題
    - [ ] 實作深層連結接受系統外部網址喚醒

---

## 全域狀態與外部系統交互
- **屬性:** 處理應用程式層級資料流與遠端整合
- 建構全域資料中心
  - **行為:**
    - [ ] 運用 Context API 解決深層元件屬性穿透問題
    - [ ] 引入 Zustand 實作更輕量的全域狀態樹
- 整合外部服務
  - **行為:**
    - [ ] 利用 axios 或 fetch 進行網路請求
    - [ ] 實作載入中與錯誤捕捉之視覺回饋
- 實作權限與本地持久化
  - **行為:**
    - [ ] 設計登入與註冊流程
    - [ ] 管理基於 Token 之驗證生命週期
    - [ ] 透過 AsyncStorage 將登入狀態寫入本地設備

---

## 後端介接與資料同步
- **屬性:** 達成全端資料連動
- 建構自訂後端服務
  - **行為:**
    - [ ] 運用 Node 與 Express 搭建應用程式介面
    - [ ] 連接 MongoDB 等遠端資料庫
    - [ ] 提供標準 RESTful 通訊介面
- 遠端資料庫同步
  - **行為:**
    - [ ] 實作應用程式資料建立與讀取與更新與刪除操作
    - [ ] 確保本地畫面狀態與伺服器資料保持同步

---

## 進階動效與微互動
- **屬性:** 提升應用程式質感與流暢度
- 導入高效能動畫方案
  - **行為:**
    - [ ] 運用 Reanimated 框架將動畫運算移轉至 UI 執行緒
- 設計進階手勢回饋
  - **行為:**
    - [ ] 透過 Gesture Handler 處理滑動刪除等複合性互動

---

## 現代架構重構與應用程式部署
- **屬性:** 確保大型專案之可維護性並對外發布
- 導入靜態型別防護
  - **行為:**
    - [x] 比較 `type` 與 `interface` 之差異與適用情境
    - [ ] 宣告 TypeScript 介面定義所有屬性與狀態型別
    - [ ] 規範 API 回傳資料之嚴格定義
- 重構專案檔案結構
  - **行為:**
    - [ ] 依據功能模組將檔案拆解歸類
    - [ ] 強制落實畫面元件與資料邏輯徹底分離
- 應用程式發布與上架
  - **行為:**
    - [ ] 執行專案打包與編譯
    - [ ] 進行蘋果與安卓雙平台商店部署流程
